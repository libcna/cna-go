# Generated runtime capability inventory

Generated from `docs/runtime-capabilities.json`; do not edit by hand.

- Profile: CNA-Go Foundations 1-13 / XNA 4.0 Windows runtime mapping
- Qualified platform: linux/amd64 (HEADLESS renderer, SDL3 audio, CNA C ABI 0.21.0)
- Capability rows: 67

| Status | Capability | Evidence | Notes |
|---|---|---|---|
| `BACKEND_BLOCKED` | Binding vertex and index buffers, and the three non-user draw calls | 20 isolated cycles against EACH of two qualified artifacts: a vertex buffer and an index buffer bound to the live device, both read back as THE SAME Go objects, three draw calls submitted, the projection's own draw guards exercised and both buffers unbound. 20 draw REFUSALS and 0 draws on each artifact. Plus 7 in-package tests, 2 behavior-corpus observations, 1 external-consumer test, 9 planted mutations (7 caught in-package, 2 by the native scenario) and 3 ABI controls | The status is BACKEND_BLOCKED for a measured reason and not a projection gap: CNA answers every draw with CNA_RESULT 12, "no effect has been applied", on both qualified artifacts -- which is the same requirement XNA's own VerifyCanDraw imposes, and CNA-Go projects no Effect surface to satisfy it with. Everything up to the draw call is VERIFIED_NATIVE: the bind reaches CNA and the buffers really are bound. The BOUND-OBJECT readers are managed, and must be: CNA hands back a handle, and a handle cannot be turned into the Go object a consumer is holding without a registry that would retain every buffer for the life of the process -- which is why the reference keeps the same two fields. Of DrawPrimitives' five guards three are reproduced and two are recorded: the ProfileCapabilities maximum has no public XNA type to read, and VerifyCanDraw's effect test is what CNA refuses on. The instance-frequency refusal IS reproduced, and it is the one guard a consumer can trip without a shader. |
| `BACKEND_BLOCKED` | Focus transitions away from the game | qualified artifact reports HEADLESS renderer with no window manager | The HEADLESS artifact raises activation once at startup and can never lose focus, so CNA_GAME_EVENT_DEACTIVATED is never delivered and the measured counter stays at zero. The Go accessors, the edge-trigger guard and the raise path are proved without it; delivery of a real deactivation is NOT_RUN_ENVIRONMENT and is not claimed. |
| `BACKEND_BLOCKED` | GameWindow client-size, orientation and screen-device signals | three native subscriptions installed and released per run, proved by the same lifetime path the four game signals use; zero deliveries measured under HEADLESS | The three canonical CNA window signals are bound through cna_game_window_subscribe, delivered on a trampoline table of their own -- the window and game identity spaces both number from zero, so one shared entry point could route a signal into the wrong projected event -- and released with the game. The HEADLESS artifact has no window manager, so it never resizes, rotates or changes screen and all three counters stay at zero. The raise paths, the routing and the handler-failure contract are proved without a delivery; an actual native delivery is NOT_RUN_ENVIRONMENT and is not claimed. |
| `BACKEND_BLOCKED` | GraphicsDevice's six events and its disposal | 15 members closed, 5 CNA routes bound with 6 native trampolines, 20 isolated cycles on EACH qualified artifact installing and removing all six subscriptions, 2 planted mutations proved to fail, 3 behavior-corpus observations and 7 newly pinned structure measurements | All six events have a raise_* protected virtual in the reference and every raise site is inside the device's own runtime code, so the canonical CNA signal IS the reference's raise path -- the same shape Game's three host-driven events have, and the opposite of Game::Disposed. Subscription is lazy: a device nobody listens to installs nothing, and the first Add installs all six at once so a partial failure cannot leave some behind. The status is BACKEND_BLOCKED and the reason is exact -- the same classification visible-rendering carries, for the same kind of reason: REGISTRATION is proved on both artifacts, DELIVERY is not, because CNA raises DeviceLost, DeviceReset and DeviceResetting only when a renderer really loses or resets a device -- which neither the HEADLESS nor the SOFTWARE artifact can be made to do -- and it raises Disposing from a disposal a scenario must not perform on a device the Game goes on using. Neither payload event reports the OBJECT: ResourceCreated fires from the graphics-resource base constructor, where CNA says the concrete type does not exist yet, and the destroyed resource's tag is caller-owned native state, so both carry nil while the destroyed resource's NAME is real and is copied out of callback-scoped bytes before they expire. Disposal really disposes the device the Game owns, which is what the reference does; the facade's ownership stays BORROWED and CNA-Go never disposes one on its own. MEASURED: CNA documents that every registration must be released before cna_game_destroy succeeds, and the pinned artifacts did NOT refuse a leaked one -- so the release is performed because the contract requires it and because leaking a C-owned resource is wrong, not because a native refusal proves it. |
| `BACKEND_BLOCKED` | Visible window rendering | qualified artifact reports HEADLESS renderer; its GameWindow reports a 0x0 client rectangle while the same run's graphics device reports an 800x480 viewport | Native draw calls are verified, but the retained artifact cannot provide visual evidence. The zero-sized client rectangle is recorded rather than asserted: a headless window has no client area, so GAME_WINDOW_POSITIVE_CLIENT_BOUNDS stays at zero and a positive size is counted rather than required. |
| `HARDWARE_PENDING` | Hardware renderer qualification | the qualification artifact is built with CNA_GRAPHICS_RENDERER=HEADLESS | Requires a separately admitted artifact built with a real renderer and visible-run evidence. CNA publishes SDL_RENDERER, SOFTWARE, OPENGL33, OPENGLES3 and VULKAN builds of the same ABI, so this is an artifact-selection question rather than an upstream gap. |
| `LANGUAGE_MAPPING_LIMITATION` | CLR Game subclassing syntax | docs/xna-go-mapping.md | Go uses a concrete Game host plus an explicit GameCallbacks adapter. |
| `PLATFORM_PENDING` | Android runtime | unsupported template entry point removed | Not claimed. |
| `PLATFORM_PENDING` | iOS runtime | no iOS runtime qualification | Not claimed. |
| `PLATFORM_PENDING` | macOS runtime | no macOS runtime qualification | Not claimed. |
| `PLATFORM_PENDING` | Web/Wasm runtime | unsupported template entry point removed | cgo/native CNA is not a Web/Wasm qualification. |
| `PLATFORM_PENDING` | Windows runtime | no Windows loader or runtime qualification | Go target compilation is not CNA runtime evidence. |
| `UNIMPLEMENTED_CNA_GO` | Audio/XACT | the qualified artifact opens a real SDL3 playback device: a direct C probe against it reported is_playback_available=1, created a 22,050-sample PCM16 mono effect, measured its duration as exactly 5,000,000 ticks (0.5s at 44.1kHz), created an instance, played it and read back CNA_SOUND_STATE_PLAYING with volume 1.0 | Foundation 1 recorded this as BACKEND_BLOCKED because its artifact was built with the NULL audio backend. That is no longer true and the classification was retired rather than restated: the current artifact is CNA_AUDIO_PLATFORM=SDL3 and playback measurably works in this environment. The only remaining blocker is CNA-Go's own surface -- SoundEffect, SoundEffectInstance and the rest of Microsoft.Xna.Framework.Audio are missing, and CNA-Go binds no cna_sound_effect_* route. AudioListener and AudioEmitter exist as pure managed descriptors and claim no runtime capability. |
| `UNIMPLEMENTED_CNA_GO` | Design-time APIs | strict structural report | Deferred. |
| `UNIMPLEMENTED_CNA_GO` | Effects, models, and 3D | strict structural report | Deliberately deferred; no BasicEffect, cube, depth-state, or fake capability API exists. |
| `UNIMPLEMENTED_CNA_GO` | Media and video | strict structural report | Deferred. |
| `UNIMPLEMENTED_CNA_GO` | Storage | strict structural report | Deferred. |
| `UNIMPLEMENTED_CNA_GO` | Touch input | strict structural report | Deferred. |
| `UPSTREAM_CNA_BLOCKED` | Native sanitizer evidence | qualified library is not instrumented | NATIVE_SANITIZER_STATUS=NOT_RUN; no leak-freedom claim. |
| `VERIFIED_MANAGED` | CLR base substitutability at a Go parameter position | the measured inventory of 51 base-typed public signature positions, of which Texture2D's 17 are the one LIVE family; a verifier cross-check in both directions; 13 widened positions; and a compile-time test that fails to BUILD if the rule is broken | In C# a parameter typed Texture2D accepts a RenderTarget2D. Go has no such relation and CNA-Go refuses to fake one with embedding, so a PARAMETER position whose CLR type is a base with a LIVE substitutability requirement projects to an exported interface with an UNEXPORTED method -- nameable by a consumer, satisfiable only inside this module. Returns and property getters keep the concrete pointer: Texture2D::FromStream returns a Texture2D and a caller uses every Texture2D member on it. The forbidden-accessor rule is untouched; there is still no Base, Parent or AsTexture2D, because the interface is a position's type rather than a conversion a consumer performs, and the call site reads exactly as the C# one does. An inherited member whose Go projection is already a PACKAGE FUNCTION -- a static, or a generic instance method under the generic-method rule -- is not re-projected on the derived type: its Go identity names its declaring type and its receiver-first parameter carries the interface, so one function serves every derived value. Registry and measurement are cross-checked: a base that becomes LIVE and is not registered leaves positions no derived value can reach, and one registered without being LIVE widens a signature for a substitution nothing can perform. |
| `VERIFIED_MANAGED` | BufferUsage enum metadata | docs/buffer-usage-evidence.md, API compatibility report, and behavior corpus | Only the two-literal named-int32 flags enum is verified; no vertex/index buffer, GPU behavior, buffer mapping, binding, creation, or draw support is claimed. |
| `VERIFIED_MANAGED` | ButtonState enum metadata | docs/button-state-evidence.md, API compatibility report, and behavior corpus | Only the Released=0 / Pressed=1 non-flags named-int32 enum metadata is verified; no mouse or gamepad hardware behavior, button polling, GamePadButtons/GamePadDPad/MouseState consumer, input backend, native enum mapping, or CNA ABI expansion is claimed. |
| `VERIFIED_MANAGED` | ClearOptions enum metadata | docs/clear-options-evidence.md, API compatibility report, and behavior corpus | Only the three-literal named-int32 flags enum is verified. Raw zero is unnamed; no GraphicsDevice.Clear overload, render-target/depth/stencil clear behavior, GPU support, or CNA ABI expansion is claimed. |
| `VERIFIED_MANAGED` | Game.Content, across the package cycle | 5 in-package tests, 5 behavior-corpus observations, 1 external-consumer test, 20 native identity checks inside a live LoadContent callback and 3 planted mutations proved to fail | Game::get_Content is one ldfld and set_Content is one guard and one stfld, so both are managed in full. The FIELD stays on Game, where the reference keeps it; the two accessors are package functions in the Content package because the settled cross-package cycle rule puts an ancestor-namespace member whose type is a descendant-namespace type in the descendant package, and internal/servicebridge carries the three closures that connect them with no public API and no registry that could retain a Game. The manager is created in the Game constructor, at the reference's own statement position, so its identity is fixed from construction. |
| `VERIFIED_MANAGED` | DepthFormat enum metadata | docs/depth-format-evidence.md, API compatibility report, and behavior corpus | Only the four-literal non-flags named-int32 enum is verified; no depth/stencil surface support, render target, presentation configuration, GPU/native mapping, consumer API, or CNA ABI expansion is claimed. |
| `VERIFIED_MANAGED` | DrawableGameComponent and its device-service resolution | 12 in-package tests, 6 Graphics-package tests that run the REAL cross-package bridge end to end, 6 behavior-corpus observations, and 3 external-canary tests from a module that imports both packages exactly as a consumer must | DrawableGameComponent is COMPLETE: 18 declared members plus 12 inherited from GameComponent, 30 Go identities, and it is the profile's first projected XNA-to-XNA derived type (XNA_COMPOSED_DERIVED_TYPES_PROJECTED 0 -> 1). The blocker was never the inheritance: Initialize resolves Microsoft.Xna.Framework.Graphics.IGraphicsDeviceService out of Game.Services, and the framework package cannot name that contract because the Graphics package imports it. internal/servicebridge resolves it with two function values installed from package inits -- no public API, no retained object, no import cycle, and no reflection at the call site. A program that never imports the Graphics package installs no resolver and resolves nothing, which is correct by construction rather than a gap: registering the service requires naming it. The type reaches no native code at all and is classified pure managed on that evidence; the device it hands back is native, obtaining it is not. It is also the profile's one SHIPPED IDrawable -- before this, every live IDrawable in a CNA-Go program was a consumer's own type. |
| `VERIFIED_MANAGED` | Managed Game disposal | 40 dispose-after-run cycles in native stress, each raising Disposed exactly twice from two Dispose calls and zero times from the run itself; behavior corpus and the external canary cover the component loop, the snapshot, the failure path and Dispose(false) | Game.Dispose(), Dispose(bool) and Finalize are projected from the pinned IL. Dispose(bool) copies Components to an array, disposes every element declaring the projected IDisposable member, and raises Disposed with the Game and EventArgs.Empty. It is deliberately NOT idempotent, because the reference carries no disposed flag: a second call disposes again and raises again. It reaches no native code at all, so a consumer may dispose after Run returns and still get the full managed semantics with no native handle left. Two reference steps are not reproduced and both are already-recorded deferrals: the graphicsDeviceManager disposal, whose field CNA-Go never assigns, and UnhookDeviceEvents, whose guard is the field HookDeviceEvents assigns and which base Initialize defers. |
| `VERIFIED_MANAGED` | GraphicsProfile enum metadata | docs/graphics-profile-evidence.md, API compatibility report, and behavior corpus | Only the Reach=0 / HiDef=1 non-flags named-int32 enum metadata is verified; no actual Reach or HiDef hardware support, profile selection, GraphicsDevice/GraphicsDeviceManager/GraphicsAdapter/GraphicsDeviceInformation consumer, GPU capability, renderer feature level, shader model, or CNA ABI mapping is claimed. |
| `VERIFIED_MANAGED` | The four graphics state objects and their freeze rule | 4 newly complete types carrying 41 value properties and 16 static presets, 6 in-package tests, 6 behavior-corpus observations, 6 planted mutations each proved to fail, and the BoundStateObject resource verified key-to-value against Microsoft.Xna.Framework.dll | BlendState, DepthStencilState, RasterizerState and SamplerState are one family with one architecture. Each composes a GraphicsResource and inherits its nine public members, and none holds a native object: the reference keeps its values in `cached*` managed fields and CNA models them as versioned C PODs passed BY VALUE, so ownership is MANAGED_VALUE and the composed resource's handle is nil -- which is exactly the reference's own `_internalHandle == 0` branch, where Name and Tag answer from local storage. Every setter begins with ThrowIfBound and is therefore FALLIBLE, carrying a refusal the reference really throws; every getter is one `ldfld` and carries none. The static presets are frozen FROM CONSTRUCTION, which the documented sentence does not say: the private constructor that builds each one ends with `isBound = true`, so BlendState.AlphaBlend refuses a write on an object no device has ever seen, while a consumer's own instance is mutable until bound. Every default and every preset argument is read off SetDefaults and the class initializers rather than from memory -- culling defaults to counter-clockwise, multisample antialiasing to ON, depth comparison to LessEqual, MaxAnisotropy to four, and the blend and stencil masks to -1. A static `initonly` field is one object for the life of the process, so each preset answers with the same pointer every time and carries the name the class initializer gave it. Applying a state to a device is NOT part of this row: the CNA get/set routes are a separate binding. |
| `VERIFIED_MANAGED` | PackedVector family | docs/packed-vector-evidence.md, API compatibility report, and PURE_XNA_DERIVED corpus | Complete 19-type managed closure; no CNA ABI route or runtime/hardware claim. |
| `VERIFIED_MANAGED` | Managed Foundation closures | API compatibility, behavior corpus, and family evidence reports | Geometry/transform, Color, Viewport, Curve, PackedVector, VertexElement, PlayerIndex, DisplayOrientation, BufferUsage, ClearOptions, SurfaceFormat, DepthFormat, GraphicsProfile, and ButtonState closures are locally strict-zero; five native/runtime types remain explicitly partial. |
| `VERIFIED_MANAGED` | SurfaceFormat enum metadata | docs/surface-format-evidence.md, API compatibility report, and behavior corpus | Only the 20-literal non-flags named-int32 enum is verified; no pixel/texture/render-target format support, compression, HDR, GPU mapping, consumer API, or CNA ABI expansion is claimed. |
| `VERIFIED_MANAGED` | VertexElement descriptor values | docs/vertex-element-evidence.md, API compatibility report, and PURE_XNA_DERIVED corpus | Complete three-type managed descriptor closure; no VertexDeclaration, buffer, draw, GPU, CNA ABI, or hardware capability claim. |
| `VERIFIED_MANAGED` | XNA namespace/package mapping | tools/api_compat and docs/xna-go-mapping.md | Case-preserving import paths; consumer aliases are not API. |
| `VERIFIED_NATIVE` | Go callback error/panic containment | 20 returned-error and 20 panic subprocess cycles | No panic or Go error crosses C; Run returns the stored Go failure. |
| `VERIFIED_NATIVE` | GraphicsDevice.Clear | 60/600 canary | Routes through cna_graphics_device_clear_rgba. |
| `VERIFIED_NATIVE` | ContentManager, loaded through CNA's content pipeline | 20 isolated content cycles against EACH of two qualified artifacts: the Game's own manager, a root directory round-tripped through CNA, an asset path CNA resolved, a Texture2D loaded and its four texels read back exactly, and CNA's cache answering a second load with the file deleted. Plus 9 in-package tests, 8 behavior-corpus observations, 2 external-consumer tests and 9 planted mutations, 8 of which were proved to fail | The native manager is created at the FIRST operation that needs a device, not in the constructor: CNA's cna_content_manager_create takes a callback-scoped device handle and the reference's constructor reaches nothing, so eager creation would refuse a constructor the reference accepts anywhere. The load set is CLOSED at *graphics.Texture2D, and a T outside it is refused by name BEFORE any device is resolved -- SpriteFont, SoundEffect and TextureCube each have a CNA route and no Go type yet, so each is a missing TYPE rather than a missing loader. Two divergences are recorded rather than hidden: OpenStream appends the reference's .xnb suffix while CNA's loader resolves <root>/<name> and decodes a loose image, and ReadAsset goes through the same cached route the reference reads past. A loaded texture is INDEPENDENTLY owned -- CNA states it remains valid across unload and destruction -- so the projection destroys it separately and never through the manager. |
| `VERIFIED_NATIVE` | The device's two texture and two sampler collections | 2 newly complete types and 4 GraphicsDevice members, 20 isolated device-state cycles against EACH qualified artifact covering identity, range refusals and both round trips, 4 planted mutations proved to fail, and 2 behavior-corpus observations | Each collection is one indexed property whose constructor is `assembly`, so CNA-Go declares none and only a GraphicsDevice hands one out. The device answers with the SAME collection object every call, which is what the reference's fields promise. The indexers refuse an out-of-range index with the reference's own ArgumentOutOfRangeException -- constructed from the parameter name with no message -- and the refusal must be the PROJECTION's, because CNA refuses out-of-range slots too and an off-by-one bound would still refuse through the wrong guard. The one measured divergence is the texture getter: XNA reads the D3D slot and recovers the managed wrapper through Texture3D::GetManagedObject and its siblings, and CNA states plainly that it has NO route from a native object back to a handle, telling a binding to cache what it binds and use the slot's `bound` flag to tell 'something else owns this slot now' from 'the slot is empty'. CNA-Go does exactly that and REPORTS the third case rather than answering nil, which would say empty, or answering a stale cache, which would name the wrong texture. The getter's declared type is the BASE, `Texture`, so a Go consumer receives the composed *Texture with the two members the CLR's static type gives and has no downcast to recover the Texture2D -- a GO_LANGUAGE_LIMITATION at a base-typed RETURN, recorded rather than worked around by widening the return and taking those two members away. |
| `VERIFIED_NATIVE` | DisplayMode | 20 isolated device-state stress cycles reading the live device's mode and checking its two computed members against the dimensions CNA reported; plus 4 in-package tests, 2 behavior-corpus observations and 1 external-canary test | The profile's first COMPLETE type whose every instance is native-sourced and whose every member is pure managed -- and those are different facts, which is why it is registered in pureManagedTypes while GraphicsDevice.DisplayMode, the member that reports one, carries the error. The contract declares six members and NO constructor, so a consumer never builds one. AspectRatio guards BOTH dimensions, which is the reference's own guard and not a redundant one: a zero width divides cleanly to 0.0 anyway, and the branch is what makes the answer defined rather than a floating point result. TitleSafeArea is the whole area, because Viewport::GetTitleSafeArea on this profile is ten bytes building a Rectangle from its four arguments. ToString renders the SurfaceFormat by NAME, as String.Format does for a boxed enum, through an unexported name table -- an exported ToString on the enum would be public API the contract does not declare. CNA reports an aspect ratio of its own and the projection deliberately does not use it: a second computed value could disagree with the reference's defined answer. |
| `VERIFIED_NATIVE` | Display orientation configuration storage | docs/display-orientation-evidence.md, API compatibility report, behavior corpus, and 20 isolated graphics-manager stress cycles in which the stored value reaches CNA's own manager | DisplayOrientation flags and GraphicsDeviceManager.SupportedOrientations storage/dirty semantics are verified. Foundation 48 changed what the setter DOES: it used to store a value that reached nothing, and now pushes to CNA's manager like its eight configuration neighbours, so it gained an error result while the getter stayed a field read. Actual screen or window rotation is still not claimed. |
| `VERIFIED_NATIVE` | Native Game event bridge | 100 isolated deliveries each of Exiting and the native disposal signal and 80 of Activated in native stress, in order and on the owner goroutine, across 80 runs including 20 second-run cycles on one Game; plus 80 managed Disposed raises across 40 dispose-after-run cycles and a measured zero raised from any run | Game.Activated, Game.Exiting and Game.Deactivated are bound to the pre-existing canonical CNA signals through cna_game_subscribe, because the reference raises all three from GameHost and the native runtime plays GameHost's part. Game.Disposed is NOT: the reference raises it from managed Dispose(bool) and from nowhere else, so Foundation 39 moved it there and CNA_GAME_EVENT_DISPOSED became a LIFECYCLE_ONLY signal that is still bound, delivered and counted -- it is what proves a registration outlives cna_game_destroy -- and raises nothing public. Exactly one native subscription per event per Game; the registration handle never reaches the public API. A second Run on the same Game releases the first four registrations and installs four fresh ones, proved 20 times with zero crashes, use-after-free or double frees. Game.Deactivated is structurally bound and separately recorded: see game-activation-transitions. |
| `VERIFIED_NATIVE` | Frame-boundary override hooks | 20 isolated override cycles in native stress delivering begin_run and end_run exactly once each, 280 begin_draw deliveries across 40 cycles, 120 refused frames each proved to skip BOTH draw and end_draw, and 20 subset cycles in which a Game declaring only BeginDraw received none of the other three hooks; docs/foundation-35-game-frame-hook-evidence.md for the measured CNA_GameFrameHooks ordering | Game.BeginRun, EndRun, BeginDraw and EndDraw are complete as base bodies and reachable as methods, and a consumer may now OVERRIDE any subset of them by declaring the matching exported method on the object they hand to NewGame. Each canonical CNA hook is installed if and only if that override exists, so a callback object written before this existed opts into nothing and the native frame position is untouched. GameCallbacks still has exactly five members, there is no registration API and no exported capability interface, and the base is never run automatically: calling game.BeginDraw() inside an override is the Go projection of base.BeginDraw() and runs it exactly where the source says, zero times or many. BeginDraw's Boolean stays a channel separate from its error, and a refused frame skips Draw and EndDraw exactly as DrawFrame does in the reference. |
| `VERIFIED_NATIVE` | Game.Tick and Game.RunOneFrame, and the standalone native session | 20 isolated frame-step cycles and 20 Run-adoption cycles in native stress: 80 Ticks, 40 RunOneFrames, exactly 20 initializations (one per session), 120 Update and 80 Draw deliveries, 20 suppress-draw proofs, 20 post-Exit proofs, 20 non-owner-goroutine refusals, 20 in-callback refusals, 60 session-lifetime checks, 20 disposal checks and 20 recreation checks | The historical blocker was that a native game existed only inside the blocking Run, so neither member had a reachable moment. That was CNA-Go's architecture rather than CNA's: cna_game_create and cna_game_destroy bracket the game and cna_game_run is a separate call, so Foundation 47 split the SESSION out of Run. The first Tick or RunOneFrame creates the session on the calling goroutine, which owns its OS thread for the session's whole life; Run adopts a session a frame step created rather than making a second one, because whoever created the native game destroys it; and Game.Dispose ends a standalone session, which is the one state the reference cannot have and CNA's one-game-per-process rule makes mandatory. Measured, not assumed: a Tick on a never-run game delivers Update and Draw and NO Initialize or LoadContent, exactly as the reference's Tick does. RunOneFrame initializes first, which the reference's does NOT -- an upstream difference recorded in the Foundation 47 evidence rather than hidden, and not workable around, because CNA offers no route that runs a host frame without initializing. |
| `VERIFIED_NATIVE` | Game.IsActive | the field the native activation signals maintain, across 100 isolated cycles delivering one Activated each; plus 2 in-package tests, 1 behavior-corpus observation and 1 external-canary test | The reference's getter is `isActive && !(GamerServicesDispatcher.IsInitialized && Guide.IsVisible)`, and the guide half is UNREACHABLE rather than simplified away: both statics live in Microsoft.Xna.Framework.GamerServices.dll, IsInitialized is `packetBuffer != null`, and packetBuffer has exactly one assignment in that whole assembly -- inside GamerServicesDispatcher.Initialize, a method on a type CNA-Go projects no part of. Foundation 42 deferred this member on that assembly; Foundation 50 measured the same fact and drew the stronger conclusion. HEADLESS produces no focus transition away from the game, so DEACTIVATED_DELIVERIES stays at zero and the false direction is proved managed-side. |
| `VERIFIED_NATIVE` | Native Game lifecycle | 60/600 canary and native stress | CNA drives Initialize, LoadContent, Update, Draw, UnloadContent, and Exiting. |
| `VERIFIED_NATIVE` | Game recreation and generation rejection | 20 isolated Game recreation cycles | Each Run gets a new generation; stale facades reject deterministically. |
| `VERIFIED_NATIVE` | Game timing and presentation state | 20 isolated timing cycles in native stress: 120 settings reaching a live native loop (six per cycle), 20 non-owner-goroutine refusals, 20 argument-boundary checks, and 20 games created with a configured non-default target step | Game.TargetElapsedTime, InactiveSleepTime, IsFixedTimeStep and IsMouseVisible project as a managed field read plus a setter that pushes to the native loop, because in the reference the managed loop reads those same fields every frame and in CNA-Go that loop is native. SuppressDraw and ResetElapsedTime do the same. All six canonical operations were already exported by the pinned artifact and had never been reached from Go; the cna_game_get_* counterparts are deliberately NOT bound, because the reference reads its own field and a second source of truth could disagree. The Game's configured values are what cna_game_create is handed, replacing the literals it used to pass. The two TimeSpan setters differ by one IL instruction and the difference is preserved: InactiveSleepTime accepts zero, TargetElapsedTime does not. |
| `VERIFIED_NATIVE` | GameWindow and Game.Window | 20 isolated window cycles in native stress: 60 identity checks (three per cycle, before, during and after a run), 120 guarded-fallback checks, 80 unguarded-failure checks, 120 live reads against a real native window, 20 title-suppression proofs and 20 non-owner-goroutine refusals; plus 5 external-canary tests from a module that cannot see internal/interop | GameWindow is COMPLETE: 20 XNA members, 25 Go identities. Game.Window returns one stable identity for the Game's whole life, because Game::.ctor calls EnsureHost() and Game::get_Window is two field reads over what it assigned. Nine CNA window routes are bound; three canonical ones deliberately are NOT. cna_game_window_copy_title is unbound because GameWindow::get_Title is one ldfld over the abstract base's own field, and cna_game_window_get_current_orientation is unbound because WindowsGameWindow::get_CurrentOrientation is `ldc.i4.0; ret` -- a constant, not a platform query. SetSupportedOrientations was the one open question and the answer is that WindowsGameWindow's body is a single `ret`: the member does nothing, CNA's only orientation setter belongs to GraphicsDeviceManager rather than to the window, and forwarding to it would invent behaviour the reference does not have. Every other member follows the reference's own null-guard split: five answer a documented fallback with no native window and four report the NullReferenceException the unguarded ones produce. |
| `VERIFIED_NATIVE` | GraphicsDevice | 60/600 canary, plus 20 isolated graphics-manager stress cycles in which Game.GraphicsDevice and a DrawableGameComponent both resolve the device the manager published | Borrowed from the Game-owned GraphicsDeviceManager; never synthetic. Game.GraphicsDevice is projected as graphics.GameGraphicsDevice by the cross-package cycle rule and reproduces the reference body exactly. Foundation 49 changed what that body FINDS: GraphicsDeviceManager's constructor now registers itself under Microsoft.Xna.Framework.IGraphicsDeviceManager and an adapter over itself under Microsoft.Xna.Framework.Graphics.IGraphicsDeviceService, exactly as the reference's constructor does, so a device no longer requires a consumer to register a service of their own. With no manager the member still reports the reference's InvalidOperationException. The manager also hands out ONE GraphicsDevice facade per native generation, because get_GraphicsDevice is a field read in the reference and a fresh wrapper per call made two callers disagree about the same device. |
| `VERIFIED_NATIVE` | GraphicsDeviceManager configuration surface | 20 isolated graphics-manager stress cycles: 120 framework-typed settings and 60 cross-package settings reaching a live native manager, 20 constructor-default checks, 20 argument-boundary checks, 20 ApplyChanges, 20 ToggleFullScreen round trips and 20 non-owner-goroutine refusals; plus 8 in-package managed tests, 2 behavior-corpus observations and 1 external-canary test | Nine configuration properties, two static read-only fields, ApplyChanges and ToggleFullScreen -- 20 of GraphicsDeviceManager's 40 missing members. Every getter is a managed field read and every setter stores, raises isDeviceDirty and pushes to CNA's manager, which is the object ApplyChanges reads; the cna_graphics_device_manager_get_* routes are deliberately unbound for the settled second-source-of-truth reason. Three of the nine are typed by Graphics-package enums and therefore project into that package by the cross-package cycle rule, with internal/servicebridge carrying the raw int32 across. The six that remain need GraphicsDeviceInformation, which needs GraphicsAdapter. The constructor still requires a live native game, which the reference does not: its constructor is pure managed. |
| `VERIFIED_NATIVE` | The device service GraphicsDeviceManager publishes | 20 isolated graphics-manager stress cycles: 40 service-registration checks, 20 duplicate-registration refusals carrying the reference's exact message, 20 Game.GraphicsDevice resolutions, 20 DrawableGameComponent.Initialize resolutions, 80 raiser checks, 20 service-removal checks and 20 native device-created signal deliveries; plus 5 events and 4 raisers projected, 3 behavior-corpus observations and 1 external-canary test | The constructor registers the manager under IGraphicsDeviceManager and an adapter over it under IGraphicsDeviceService, and Dispose removes both -- and only its own, on the reference's `GetService(...) == this` guard. The adapter exists because no framework-package type can declare IGraphicsDeviceService's device accessor; it is created once per manager, so its identity is stable, and only reference equality with the manager differs from the reference. The three IGraphicsDeviceManager operations are interface WITNESSES: the reference implements them privately with three `.override`s, so the contract's public member set has none of them. The five canonical CNA manager signals are bound through a third trampoline family whose device events do not start at zero, and DeviceCreated is delivered and routed 20 times out of 20; the reset, resetting and disposing counters stay at zero because a HEADLESS device is created once and never lost. |
| `VERIFIED_NATIVE` | GraphicsDevice render state | 20 isolated device-state stress cycles: 100 write-then-read-back round trips through the live device (blend factor, multisample mask, reference stencil, scissor rectangle, viewport), 60 read-only value checks, 40 masked clears, 20 refused non-finite depths, 20 presents, 20 stale-generation refusals and 20 non-owner-goroutine refusals; plus 3 in-package tests, 4 behavior-corpus observations and 1 external-canary test | Fifteen members: BlendFactor, MultiSampleMask, ReferenceStencil and ScissorRectangle with both accessors, the Viewport SETTER whose getter shipped in Foundation 1, GraphicsProfile, GraphicsDeviceStatus and IsDisposed read-only, both masked Clear overloads, and Present(). Every one asks CNA rather than a managed cache, and that is measured rather than convenient: the reference fills those cache fields when ITS constructor creates the D3D device, CNA-Go does not create the device, so a cache here would start at Go zero values and disagree with the live device until written. The consequence is stated: five members that carry no error in the reference carry one here. The three enum identities cross unchanged -- ClearOptions 1/2/4, GraphicsProfile 0/1, GraphicsDeviceStatus 0/1/2 are the same numbers on both sides -- and a test asserts each pair by value rather than by conversion. Present() is spelled PresentByNone because Present is an overload group; the verifier reported the un-suffixed name as an UNEXPECTED_MEMBER when it was tried. |
| `VERIFIED_NATIVE` | Applying state objects to the device, and the state-carrying Begin overloads | 20 isolated device-state cycles against EACH qualified artifact: 20 null refusals, 60 binds and 40 state-carrying Begin/End pairs per artifact, plus 4 planted mutations proved to fail, 3 behavior-corpus observations and 4 newly pinned CNA structure layouts | GraphicsDevice::get_BlendState answers with the OBJECT the setter was given, and InitializeDeviceState stores null into all three cached fields -- so a device answers nil until something sets one, and the getters cannot read CNA back: CNA holds the VALUES, a POD per state, and a fresh object built from them would fail the first identity comparison a consumer writes. The cache lives on the GraphicsDevice facade, which is already one object per manager per native generation. The setter reproduces set_BlendState's four steps: the ArgumentNullException for a null, the ObjectDisposedException Apply throws first, the push to CNA, and Apply's own two stores -- the parent, then isBound -- so a state object handed to any device is read-only from then on. The effect-pass step has no counterpart and is recorded rather than skipped silently: CNA-Go maps no effect subsystem. Begin's null arguments are DEFAULTS read off SetRenderState -- AlphaBlend, LinearClamp, None, CullCounterClockwise -- and SetRenderState applies them to the device, so after Begin the device answers with them and they are frozen. CNA takes all four descriptors on one route, so the values reach the renderer once rather than through four device calls and a fifth; the managed half is performed here so the observable result is the reference's. The two Begin overloads that also take an Effect remain blocked on a shader subsystem CNA-Go maps no part of. |
| `VERIFIED_NATIVE` | The GraphicsResource -> Texture -> Texture2D base chain | 3 newly complete types, 22 inherited member projections across Texture2D and SpriteBatch, 7 in-package tests, 7 planted mutations each proved to fail, 4 behavior-corpus observations, 7 verifier identity mutations, and one native control in tools/native_stress that no managed observable can replace | The first XNA base whose composition closes a whole family. The native handle lives on GraphicsResource, exactly where the reference's `_internalHandle` lives, so there is ONE native owner per logical object and a derived wrapper never creates a second; interop.Resource carries its own kind tag, so the type-specific destruction the reference's ReleaseNativeObject overrides perform is already inside Resource.Dispose. Disposal here is IDEMPOTENT, unlike GameComponent's: ~GraphicsResource guards on isDisposed, sets it BEFORE announcing, and raises Disposing exactly once. The two overrides disagree with each other and both are reproduced as written -- Texture2D releases on the finalizer path too, because its !Texture2D passes a hardcoded ldc.i4.1 to ReleaseNativeObject, while SpriteBatch's guards on `disposing` and does not. Name and Tag are managed storage: the reference reaches DeviceResourceManager, which is a Dictionary under a Monitor answering String.Empty for an absent key, so both accessors are infallible. CNA has twelve cna_graphics_resource_* routes and Foundation 56 wrongly recorded that it had none; Foundation 57 measured every one against the pinned artifact and recorded per-route reasons for leaving them unbound -- decisively, they refuse a SpriteBatch handle, which IS a GraphicsResource here, and set_name validates UTF-8 where the reference validates nothing. Width, Height, Bounds, Format and LevelCount stopped carrying errors for the same reason, correcting a fallibility claim CNA-Go had made without evidence. The one thing no managed observable can distinguish is whether the inherited Dispose() reaches the DERIVED override or the composed base's slot -- the flag and the event are the base's either way -- so the control is native: after the derived body runs the CNA texture is destroyed and SaveAsPng reports ErrDisposed, and after the base body runs it succeeds. |
| `VERIFIED_NATIVE` | IndexBuffer, written and read back through CNA | 20 isolated index-buffer cycles against EACH of two qualified artifacts: two buffers created, the description CNA applied checked, indices written and read back exactly whole-array and through a window, the projection's own refusals exercised, the WriteOnly read refused and disposal proved. Plus 12 in-package tests, 4 behavior-corpus observations, 1 external-consumer test, 13 planted managed mutations proved to fail and 6 ABI controls | Both constructors refuse a non-positive count BEFORE storing the device, which is the reference's order. A nil device is refused in Go's own words rather than borrowing DeviceCannotBeNullOnResourceCreate: IndexBuffer's constructors have no device check at all -- they dereference two statements later, so C# gets a NullReferenceException. The Type constructor is NARROWER than Marshal.SizeOf: CNA stores 16-bit or 32-bit indices and nothing else, so the accepted set is closed at int16/uint16/int32/uint32 and anything else is refused by name. Helpers::ValidateCopyParameters is reproduced with its three parameter names and its order. The one CopyData check NOT reproduced is the still-bound test, because GraphicsDevice::Indices is not projected yet and there is no way to ask; that is recorded rather than skipped. All three properties record what CNA APPLIED rather than what was requested, so a renderer that widened an index would be visible. |
| `VERIFIED_NATIVE` | Keyboard state | 60/600 native callback polling and PlayerIndex overload stress | Both GetState overloads use the same existing CNA process-keyboard snapshot; PlayerIndex is ignored as in XNA IL, and HEADLESS naturally reports no keys. |
| `VERIFIED_NATIVE` | Linux amd64 runtime | admitted-range ABI native gates (major 0 with minor 21 or newer, qualified at 0.21.0), stress, and canary | Requires cgo, a C compiler at build time, and a separately supplied CNA shared library. The admitted range is CNA's own contract, not a relaxation: CNA states that its ELF symbol-version node changes only for a major ABI break and that minor releases are additive. |
| `VERIFIED_NATIVE` | Owner OS-thread policy | native stress wrong-thread rejection and owner retry | Run locks one OS thread through native destroy and callback-handle deletion. |
| `VERIFIED_NATIVE` | RenderTarget2D, bound and read back | 20 isolated render-target cycles against EACH of two qualified artifacts: create, bind, clear, unbind, read back and draw. The SOFTWARE renderer produced 20 pixel readbacks of 64 pixels each, every one the exact cleared colour; the HEADLESS renderer produced 20 documented readback refusals and completed every other step. Plus 5 in-package tests, 4 behavior-corpus observations and 2 planted mutations proved to fail | The whole semantic slice, in one draw callback, ending in a readback THROUGH THE Texture2D SURFACE -- which proves the substitution and the render-target contents with one call. CNA reports the APPLIED format, depth format, sample count and usage rather than echoing the preferences back, which is the same split the reference has: CreateRenderTarget hands its arguments to GraphicsAdapter::QueryFormat and the RenderTargetHelper stores what the query selected. A render target is a DISTINCT CNA kind with its own destroy -- cna_texture2d_destroy is documented as not destroying one -- so the composed GraphicsResource carries the render-target kind and destroys it through the matching route. The two renderers differ on exactly one step: only a renderer that can copy a colour attachment back to the CPU can answer GetData, and the HEADLESS artifact says so with CNA_RESULT_NOT_SUPPORTED. ContentLost is projected and cannot fire in either qualified environment: CNA raises it only on the DIRECTX9, DIRECT2D and SKIA renderer families, which never lose a device on the other backends. |
| `VERIFIED_NATIVE` | SpriteBatch 2D draw closure | 60/600 scaled draw canary, 20 lifetime cycles, and 20 isolated sprite-draw cycles submitting 100 position commands and 80 destination commands through a live draw callback | All SEVEN of the profile's Draw overloads are projected, and they reach TWO native routes: the four position overloads go to cna_sprite_batch_submit_scaled_many and the three destination-rectangle overloads to cna_sprite_batch_submit_many. CNA declares the two commands separately because the position family measures its origin in source-texture pixels and applies the scale after that offset, which a caller cannot reproduce by computing a rectangle; XNA agrees from the other side, with one InternalDraw whose Vector4 destination means (x,y,scaleX,scaleY) or (x,y,width,height) depending on a scaleDestination bool. Begin, End and disposal are real, and Foundation 56 composed GraphicsResource onto the type, so its ten inherited members -- GraphicsDevice, Name, Tag, IsDisposed, ToString, Disposing and the two-overload Dispose pair -- are projected too. The XNA type is still partial: its four state-carrying Begin overloads need BlendState, SamplerState, DepthStencilState, RasterizerState and Effect, and its six DrawString overloads need SpriteFont. |
| `VERIFIED_NATIVE` | The SpriteBatch begin/end pair and its guards | 20 isolated sprite-draw cycles: 20 nil-texture refusals, 40 outside-pair refusals and 40 pair-guard refusals, each carrying the reference's exact message; plus 6 in-package tests, 5 behavior-corpus observations and 2 external-canary tests | inBeginEndPair is tracked managed-side, as the reference tracks it, because the reference's three throw sites report Microsoft's sentences and CNA -- which refuses the same states with CNA_RESULT_INVALID_STATE -- reports its own. The order is the IL's: InternalDraw throws ArgumentNullException("texture", NullNotAllowed) at IL_0003, before it reads inBeginEndPair at IL_0014, so a nil texture outside a pair reports the argument. CNA-Go's own disposal check runs after both, which is also the reference's shape: a disposed SpriteBatch still holds the field. Begin raises the flag only after CNA accepts and End clears it only after CNA accepts, so a refused call leaves the pair exactly as it was. |
| `VERIFIED_NATIVE` | Texture2D creation from dimensions | 60 empty textures created across 20 isolated cycles -- the three-argument constructor, the five-argument one with the same defaults, and one with a mip chain -- each read back for its dimensions and disposed; plus 40 refusals, 2 behavior-corpus observations and 1 external-canary test | Both constructors are thirty bytes of IL over one private CreateTexture, so the three-argument one is the five-argument one with mipMap false and SurfaceFormat.Color, and those two defaults are read off the IL rather than chosen. The reference's usage and pool arguments are D3D constants CNA has no parameter for. The one guard reproduced is the reference's first: a null device carries FrameworkResources.DeviceCannotBeNullOnResourceCreate. Everything after it -- a zero dimension, a format the renderer does not have -- is CNA's refusal reported as CNA states it, because reproducing D3D9's format-capability messages would mean asserting a support decision CNA-Go did not make. A negative dimension is refused by the projection rather than converted into an enormous uint32. |
| `VERIFIED_NATIVE` | Texture2D stream load and save | 128x128 PNG dimensions and 20 texture cycles, plus 40 encodes and 40 sized decodes across 20 isolated cycles -- the encodes checked for the PNG signature and the JPEG SOI marker on bytes CNA produced, the decodes required to come back at the requested 24x24 in both zoom modes | Both directions now: the sized FromStream decodes at a requested width and height with the reference's zoom distinction (cover-and-crop when true, fit while preserving the aspect ratio when false), and SaveAsPng/SaveAsJpeg encode into an io.WRITER. The Stream direction is the one place the profile's default BCL mapping was wrong -- System.IO.Stream maps to io.Reader, which a consumer cannot use to receive bytes -- so tools/api_compat now carries a per-member registry that records which Stream positions are written, measured from the member body rather than derived from the signature. The two image-format identities do NOT cross unchanged: XNA numbers Jpeg 0 and Png 2, CNA numbers PNG 0 and JPEG 1, and forwarding XNA's constant would encode a JPEG under SaveAsPng with nothing about the call reporting it. The io.Reader is read from its current position and CNA-Go does not close it; a nil destination carries FrameworkResources.NullNotAllowed. The reference's second guard, !stream.CanWrite, has no Go counterpart and is recorded rather than invented. |
| `VERIFIED_NATIVE` | Texture2D typed pixel transfers | 60 write-then-read-back round trips across 20 isolated cycles -- a full-surface Color transfer, a windowed one and a rectangle one per cycle -- plus 40 refusals, 5 in-package tests, 3 behavior-corpus observations and 1 external-canary test | The profile's first users of the generic-method projection rule. Go METHODS CANNOT DECLARE TYPE PARAMETERS, so a CLR generic instance method has no method-shaped projection at all: it becomes a package-level function taking the receiver first, the shape the cross-package cycle rule already established. The overload suffix names the type parameter the member DECLARES -- the contract's `!!0` is an IL token for a POSITION, and until Foundation 54 the verifier expected `SliceOf0`. The element mapping is closed over CNA's eighteen CNA_TEXTURE_DATA_* identities, every one of which has a Go type, and each Go type's size is checked against the width the identity means: CNA identifies an element by what it represents and never by how large it is, so a Go type whose layout drifted would be copied into a buffer read with a different stride and nothing on either side would report it. The CLR constrains T as a struct with a default constructor; Go has no such constraint, so T is `any` and the element type is refused at runtime by name. GetData reports rather than truncating when CNA says the transfer needs more elements than the destination holds. |
| `VERIFIED_NATIVE` | VertexBuffer, written and read back through CNA | 20 isolated vertex-buffer cycles against EACH of two qualified artifacts: two buffers over one shared declaration, its CNA handle created once and reused, four vertices written and read back exactly, two more read from a byte offset into the buffer, a third buffer built from a consumer's own IVertexType, a padded 32-byte-stride declaration proved, and the projection's own refusals. Plus 13 in-package tests, 3 behavior-corpus observations, 1 external-consumer test, 13 planted mutations (12 caught in-package, 1 by the native scenario) and 6 ABI controls | The transfers use CNA's RAW routes rather than its typed ones, and that is the faithful choice: XNA's SetData<T> takes any struct and strides by sizeof(T), which is exactly what a raw upload describes, while the typed route would close T to a set the reference does not. VertexDeclaration::FromType is reproduced check for check, which is why IVertexType is projected at all -- without it the Type-keyed constructor would be a member nothing could satisfy. TWO narrowings are measured and recorded rather than hidden: a vertexStride LARGER than sizeof(T) is refused because CNA publishes no route that writes a strided window while leaving the gaps, and XNA leaves them; and a byte count that is not a whole number of the buffer's own vertices is refused because cna_vertex_buffer_set_data_raw_at rejects any stride but the buffer's, by name. The declaration's CNA handle is created lazily, on the first buffer that needs one, and passes the STORED stride explicitly -- CNA's stride-less route would recompute it and silently replace an explicit one. |
| `VERIFIED_NATIVE` | VertexDeclaration and its element validator | 15 in-package tests including the validator's seven refusals, 7 behavior-corpus observations, 1 external-consumer test, 12 planted mutations proved to fail, and 20 isolated native cycles against each of two qualified artifacts in which its CNA handle is created once and shared by two buffers | Its PUBLIC surface is managed, and that is the reference's own shape rather than a deferral: the constructors clone the element array, store a stride and call VertexElementValidator, and GraphicsResource::_parent is assigned only by the internal Bind -- so a constructed declaration reports a NULL GraphicsDevice, as it does in XNA. VertexElementValidator is reproduced check for check and IN ORDER, because the order decides which of two simultaneous defects a caller is told about; its five FrameworkResources sentences are registered and validated key-to-value against the retained assembly. Foundation 64 bound NONE of CNA's declaration routes because nothing consumed one; Foundation 66 gave them a consumer -- cna_vertex_buffer_create takes a declaration handle -- so the handle is created LAZILY, at the first buffer that needs it, and always with the STORED stride: CNA's stride-less route would recompute one from the elements and silently replace an explicit stride the two-argument constructor was given. |
| `VERIFIED_NATIVE` | Viewport | native viewport 800x480 observed in qualified artifact | Read from CNA; no hard-coded public dimensions. |
